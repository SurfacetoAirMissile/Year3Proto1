using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WindCannonShared
{
    protected float windCannonForce;

    // Start is called before the first frame update
    void Initialize()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
